package Event;

/**
 * Event types supported by the simulator.
 */
public enum EventType {
    TRAFFIC_LIGHT_CHANGE,
    NEW_VEHICLE,
    VEHICLE_SIGNAL_ARRIVAL,
    VEHICLE_ROAD_ARRIVAL,
    VEHICLE_DEPARTURE,
    VEHICLE_EXIT
}
